# Optimized requirements.txt for Render deployment
# Web framework
fastapi==0.100.1
uvicorn[standard]==0.23.2
pydantic==1.10.12

# ML libraries (CPU versions for better compatibility)
tensorflow-cpu==2.13.0
scikit-learn==1.3.2
numpy==1.24.3
pandas==2.0.3
joblib==1.3.2

# HTTP client
requests==2.31.0

# Environment management
python-dotenv==1.0.0

# For health checks (if using curl in Dockerfile)
# Note: curl should be installed via apt-get, not pip
